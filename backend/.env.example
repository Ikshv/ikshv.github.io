PORT=4000
JWT_SECRET=replace-with-long-random-string
JWT_EXPIRES_IN=7d

# Comma-separated browser origins allowed to call this API (no trailing slashes)
# Add your Vercel preview URL if needed, e.g. https://your-app.vercel.app
CORS_ORIGINS=http://localhost:3000,https://ikshv.github.io,https://isaacshvartsman.com
