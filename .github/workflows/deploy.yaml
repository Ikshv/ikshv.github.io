name: Deploy to GitHub Pages

on:
  workflow_run:
    workflows: ["Generate Project Index"]
    types:
      - completed

jobs:
  build-and-deploy:
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout dev branch
        uses: actions/checkout@v4

      - name: Install dependencies
        working-directory: frontend
        run: npm install

      # Supabase URL + anon key come from frontend/.env.production (public client config).
      # Do not inject empty secrets here — unset env would override dotenv and break the build.
      - name: Build the site
        working-directory: frontend
        run: npm run build

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: frontend/build  # <--- this is the important change
          publish_branch: gh-pages
