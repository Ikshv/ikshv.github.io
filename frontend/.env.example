# Copy to .env.local for overrides. CRA embeds these at build time.
#
# Local API (run backend: cd backend && npm start)
REACT_APP_API_URL=http://localhost:4000
#
# Production: set this in Vercel / hosting to your deployed API base URL (no trailing slash)
# REACT_APP_API_URL=https://your-api.example.com
